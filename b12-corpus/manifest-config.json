{
  "_readme": [
    "THE PILOT'S CONFIG. Build with:  node scripts/b12-manifest.mjs plan b12-corpus/manifest-config.json --pilot-only",
    "Keys beginning with _ are ignored by parseManifestConfig; they are notes for whoever fills this in.",
    "",
    "EVERY PIN BELOW IS IN ONE OF THREE STATES, and the distinction is the point:",
    "  KNOWN HERE  - computed on the authoring machine and already filled in.",
    "  MAC-LOCAL   - null, because only the run machine can know it. The tool REFUSES on null;",
    "                a plausible-looking placeholder would VALIDATE, which is worse than a refusal.",
    "  CHOSEN      - the two constants thresholdArgument says have no ancestor. DECIDED 2026-08-12,",
    "                pre-data, with the reasoning and the refutation attempt in _chosenConstants.",
    "",
    "Do not replace a null with a guess to make the tool quiet. The refusal is the deliverable.",
    "Every remaining null is MAC-LOCAL. Nothing here is waiting on a decision."
  ],

  "specRoot": "b12-corpus",

  "pilotRunId": "2026-08-13-mac-b12-pilot",
  "_pilotRunId": "Rename to the date the pilot actually runs. It names evidence/<pilotRunId>.b12.pilot-<taskId>.manifest.json and, after the run, evidence/<pilotRunId>.b12.pilot.json.",

  "pilot": [
    "pilot-normalizeid",
    "pilot-usablefree",
    "pilot-csvreturn",
    "pilot-csvcomment",
    "pilot-lmsguard"
  ],
  "_pilot": "ORDERED, and the order is load-bearing: committedOrderViolation runs for every treatment arm and is NOT under the pilotMode exemption, so each of these ships as its own single-task manifest with its task at index 0. All five are excluded from manifests A and B by design.artifacts 4.",

  "_manifestsAandB": "ABSENT ON PURPOSE. runPlan PHASE 2 precedes PHASE 4 — the pilot runs before the sealed manifests exist. --pilot-only is what expresses that; without it the tool would demand 60 authored siblings before emitting five pilot manifests.",

  "pinned": {
    "ratesSha256": "6d74cafd7fa5702352d068825be2f75f3ae7dd0ef3df77cc9ff7f04826730c94",
    "b12RunSha256": "053eb27932e441cc37e19c1679942533e6ff801c852a0cfbb294f9287214b315",
    "claudeMdSha256": "a81a02652ccefaac797cbe524ca545e042add2c323bca9105d5a01a2c3535d2a",
    "scoringCommand": "node dist/cost/b12/emit.js 2026-08-13-mac-b12-pilot",
    "perArmTimeoutMs": 2700000,
    "extraArgs": [],

    "claudeCodeVersion": null,
    "claudeBinarySha256": null,
    "clientTruncationCap": null,
    "settingsSha256s": { "settings": "75cd35da2e1975836d06e7bd2436e88c37cb265c6f25385c4eed0835ca4d867d", "settingsLocal": null },
    "installedCharsProbe": null,
    "installedCharsProbeSha256": null,
    "mcpConfig": null,
    "mcpConfigSha256": null,
    "policyBlobs": { "treatment": null, "control": null },
    "memorySnapshot": null,
    "memorySnapshotSha256": null,
    "captureSha256": null,

    "pacingCacheWriteShareCeiling": 0.95,
    "perTaskDenominatorShareCap": 0.25
  },

  "_knownHere": {
    "ratesSha256": "sha256 of .local-coder/rates.json at the shared green parent. Compared inside EACH base commit's own tree by assertRatesFrozen, so it is a property of the corpus, not of a machine.",
    "b12RunSha256": "sha256 of scripts/b12-run.mjs at 608e930. GOES STALE THE MOMENT THAT FILE CHANGES — observe asserts the running harness IS the sealed one, so recompute if you touch it before the run.",
    "claudeMdSha256": "sha256 of the in-repo CLAUDE.md at 608e930.",
    "scoringCommand": "voidConditions 19: the ONE string committed at pre-registration. Change it here and nowhere else.",
    "perArmTimeoutMs": "45 minutes. Declared rather than left to the silent fallback, which manifestDeclarationGaps refuses.",
    "extraArgs": "Empty, deliberately: a non-empty value forces an installedChars RE-PROBE (b12-run.mjs:1911)."
  },

  "_macLocal": {
    "claudeCodeVersion": "The FULL x.y.z on the run machine. assertPinned compares with .includes(), so a partial pin like 2.1.2 silently matches 2.1.221 — the assembler refuses anything that is not x.y.z.  claude --version",
    "claudeBinarySha256": "sha256 of the Claude Code binary that will run.  shasum -a 256 $(which claude)",
    "clientTruncationCap": "The cap MEASURED for that version. voidConditions 8 VOIDS a run with no cap measured for the version that ran.  scripts/b12-truncationcap-probe-mac.sh",
    "settingsSha256s": "BOTH keys required, and null is a legitimate value meaning 'this file does not exist' — omission declares nothing, null declares an absence. NOT MAC-LOCAL, which is why `settings` is filled: instructionHashesAt hashes .claude/settings.json out of each ARM's detached worktree (b12-run.mjs:2815), and those worktrees come from the corpus tags, so this is a property of the published trees and is knowable here. MEASURED across all 65 b12/corpus/* tags: every one carries .claude/settings.json as blob 34994e4f, 432 bytes, sha256 75cd35da…4d867d; none carries .claude/settings.local.json, so settingsLocal stays null and that null is TRUE. It was null before, which declared an absence that is false. Mechanically it would have sealed either way — b12-run.mjs:1068 shape-checks the keys and nothing ever compares these values to instructionHashesAt — so this is a correction to what the file SAYS, not a refusal that was being dodged.",
    "installedCharsProbe": "Path to the committed probe artifact, plus its sha256. The re-probe is OWED with the orchestrator model in the calibration key (PREMISES.md, sixth decision).  scripts/b12-installedchars-probe-mac.sh",
    "mcpConfig": "Path to the generated MCP config, plus its sha256. observe dereferences both and NO validator checks them; the path is machine-local, so presence is all this machine can assert.",
    "policyBlobs": "One {repo, commit, path, sha256} per arm, for the OUT-OF-REPO policy bundles. The repo must exist on the run machine, be a full (not shallow) clone, and resolve OUTSIDE this repository — findPolicyBlob refuses an in-repo path as in-repo policy wearing an out-of-repo name.",
    "memorySnapshot": "Directory of the memory snapshot, plus its sha256. FOUND LATE AND WORTH SAYING WHY: observe REQUIRES this (b12-run.mjs:1690, citing voidConditions 13) and manifestDeclarationGaps never asks for it — so before the assembler was taught to check it, a manifest could pass every build-time check in this repository and then refuse on the run machine.",
    "captureSha256": "sha256 of the built dist/ capture. Its absence is SILENTLY PERMISSIVE rather than refusing: observe compares only `if (want)` (b12-run.mjs:2040), so leaving it null-and-unfilled means the capture ships unverified. STATE.md's standing note — re-pin this at the seal — is the same instruction from the other end."
  },

  "_chosenConstants": {
    "_status": "DECIDED 2026-08-12, pre-data, before any B12 observation exists. thresholdArgument calls these the two numbers 'this design must set that have no ancestor' and requires them 'declared as chosen rather than derived'. Both values below were adversarially reviewed against nine enumerated claims and SEVEN of the nine were sustained against the author; the ceiling moved from 0.85 to 0.95 as a result. What survives is recorded here, corrections included, because a justification that hides its refuted version is worth less than the number it defends.",

    "pacingCacheWriteShareCeiling": {
      "value": 0.95,
      "what_it_does": "admissionRule 11's second half. pacingFacts (src/cost/b12/assemble.ts:150-158) prices ONLY the observation's originatedRequestIds (:588-589) and fires on a STRICT >. voidConditions 20 then lifts it from an observation-level disposition to a run-level one: buildArchiveChecks pushes the clause when ANY assessed observation exceeded (:1187-1198), and assemble.ts:454-455 sets verdict='void' when ANY archive check fired. So one observation over this ceiling VOIDS ALL ~60 SESSIONS, and the experiment has two attempts total. Note `assessed`, not `admitted`: an observation already dropped for task_failed still counts here, and the only escape is an unreadable observation.json or an unrebuildable transcript (:567-586).",
      "the_finding_that_set_it": "THE STATISTIC CANNOT SEPARATE THE TWO CASES IT IS ASKED TO SEPARATE. A legitimately SHORT observation is structurally near the top of the range: its first request writes the whole context and nothing has re-read it yet. Measured over per-session groups on this machine: 1 request -> 0.8841, 2 requests -> 0.7732, 3 -> 0.7610. An all-rewrite PATHOLOGY at a long session lands at 0.93-0.98. The bands OVERLAP, so no value is safe in both directions. Given that, the ceiling is set above the legitimate-short envelope, because the erring-low error costs a paid attempt immediately while the erring-high error is covered by the gap half -- which catches the phone-call scenario admissionRule 11 actually names, directly and independently.",
      "why_not_0_85": "That was this file's first answer and it is wrong. It was chosen against a population filtered to >=2 requests, which excluded the very shape that binds. Nothing guarantees a B12 observation makes ten requests, or two: validity requires only a nonzero originated count (scripts/b12-run.mjs:2988-3012). A single-request observation measured 0.8841 here, so 0.85 would have voided the run on a legitimate one.",
      "why_not_higher_still": "It must remain a check. With cacheWrite at the 1h class's 2.0x and output at 5.0x, an all-rewrite session has share 2N/(2N+5o+i); solving strictly with o=1500, i=200 gives N > 21,816.7 at c=0.85, N > 34,650 at 0.90 and N > 73,150 at 0.95. At the 5m class's 1.25x the same three are 34,907 / 55,440 / 117,040. So 0.95 still fires on a 45-minute coding observation rewriting a 73k context at 1h rates; it is weak at 5m rates, and that is exactly the case where the gap bar drops to five minutes and the gap half becomes twelve times more sensitive. The two halves cover each other in opposite regimes.",
      "the_error_this_takes": "NOT that erring high is free -- an earlier draft claimed that and it is false. Undetected inflation lowers R, and a lower R makes a FALL easier, which the design's bracket reading (R_hi+ for the fall) exists to prevent. Separately, inflation in the CONTROL arm raises R_ab = sum(A_control - A_treatment)/sum(A_control) -- and no value of this constant covers that, because primary assembly filters to arm === 'treatment' (assemble.ts:221-222) and control observations are never pacing-assessed at all. The error is taken knowingly, in exchange for not spending a paid attempt on a false positive."
    },

    "perTaskDenominatorShareCap": {
      "value": 0.25,
      "what_it_does": "NOTHING MECHANICAL, and that is checked, not assumed: perTaskDenominatorShare is computed at assemble.ts:845 and typed at types.ts:1028, and NO code anywhere reads perTaskDenominatorShareCap -- the only mention outside this file is the comment at assemble.ts:843 saying it decides nothing, 'a live predicate here would mint a void the frozen text never wrote'. manifestDeclarationGaps still REQUIRES it (b12-run.mjs:1061-1064), so it is a pre-commitment published on the manifest's face and compared by a reader, not by the scorer.",
      "why_0_25": "Because it costs nothing when it fires, it is chosen tight and meaningful rather than safe. The denominator is admittedSumAPlusSLo over the ADMITTED set, which assemble.ts:389-405 caps at 20 treatment observations, one per task id -- so the EQUAL share is 1/20 = 0.05 and 0.25 is five times it. STATED WITH ITS CONDITION, because the review sustained the correction: shares are weighted by each task's A+S_lo, not by observation counts, so '0.25 equals a minimum evaluable stratum of MIN_DELIVERY_OBSERVATIONS = 5' holds only if those five weigh the same as the others. What is unconditionally true is the multiple: 0.25 is 5x the equal share, and a task at it carries the weight five average ones would.",
      "expected_to_fire": "Yes, sometimes, and that is the intent. If one task costs 10x the median while the rest sit at it, its share is 10/29 = 0.34 and the reader is told so. A covariate that never fires reports nothing."
    },

    "_refutation": {
      "predicted_before_measuring": "Written to a file before any measurement ran. Median cacheWrite share 0.15-0.45 (HELD, 0.3042); p95 below 0.80 (HELD, 0.6916); a healthy session above 0.85 (recorded FAILED, then TRUE after all -- the first pass filtered to >=2 requests and hid the single-request case at 0.8841); max above 0.90 (FAILED, 0.8841 is the highest measured). The pre-registered falsification rule said that if nothing healthy sat above 0.85 then 0.95 was unearned slack and had to be dropped. It was dropped -- and then restored, when the excluded subpopulation turned out to be the one that binds. The prediction's ARITHMETIC about short sessions was right; its POPULATION was wrong, and the second error hid the first.",
      "method": "cacheWrite units / total billed units over 160 Claude Code transcripts, replicating readUsage's 'equal or nothing' TTL split (src/cost/transcript.ts:201-241), rates.json's multipliers, api-error exclusion, one row per requestId. Run twice: per FILE, then per sessionId -- the second because pacingFacts prices only originatedRequestIds, and a whole-file group can carry inherited requests it would exclude. Grouping tighter did not move the tail (max 0.7732 either way for >=2 requests), which is the answer to that attack rather than an assumption about it.",
      "what_this_is_not": "Not derived from any B12 observation -- none exists. But NOT 'unfitted' either, and the review was right to press: the historical data set the lower edge of the window this value sits in, and the arithmetic set the upper edge. The value is chosen inside that window. That is the honest label, and it is narrower than the first draft's claim."
    }
  },

  "_theGapHalfIsTheBiggerRisk": {
    "_why_this_block_exists": "The OTHER half of admissionRule 11 -- max inter-request gap > the shortest cache TTL in play -- has NO constant to choose and is a larger threat to a paid run than either number above. It is recorded here because deciding the two constants is not the same as covering pacing, and reading this file could otherwise leave the opposite impression.",
    "the_bar_is_5_MINUTES_whenever_anyFiveMin_is_set": "pacingFacts sets anyFiveMin when ANY owned request has cacheWrite5m > 0, and TTL_MS then selects 300,000 ms over 3,600,000 (assemble.ts:147-148). TWO INDEPENDENT ROUTES SET IT, and neither is under this file's control:",
    "route_1_a_subagent": "ALREADY MEASURED ON THE MAC, today: MEASUREMENTS.jsonl:239 (run 2026-08-12-mac-b12-subagentkey-77fddca) records 9,397 subagent cache_creation tokens in the 5m class against 0 on the main thread, whose writes were 1h. So an observation carrying a subagent gets a five-minute pacing bar. That row also records that this FALSIFIED the earlier reading from the installedChars probe, which saw 7 records with no ephemeral_5m and concluded pacing was probably defused -- those 7 had no subagent. n = 1 subagent, one session: not established that every subagent writes 5m.",
    "route_2_an_inconsistent_split": "MEASURED HERE and not previously named anywhere: readUsage returns cacheWrite5m = total when the TTL split disagrees with its own top-level cache_creation_input_tokens (transcript.ts:232-238), so an INCONSISTENT record books its whole write to the 5m class and sets anyFiveMin -- with no real 5m write involved. 144 of 81,979 assistant records in this machine's corpus (0.18%) have such a split. One of them inside an observation's owned set is enough to drop that observation's bar to five minutes.",
    "what_that_costs": "On the 1h bar, only 0.44% of 33,894 historical inter-request gaps exceed it and the 15 largest are 18h-186h RESUMES, which a harness-spawned session cannot have -- observe mints a fresh --session-id per observation and never resumes. On the 5m bar, 4.01% of the same gaps exceed it. voidConditions 20 makes ONE exceeding observation void the whole run.",
    "what_is_NOT_established": "That a harness-driven, non-interactive observation idles the way a human-driven session does. The historical gaps include a human thinking and typing; a B12 observation has no human in the loop and its gaps are tool-execution times. This is the single largest unquantified risk to the paid run, and it is not resolvable from Windows -- but it is also not resolvable by choosing either constant above, which is the point of recording it here rather than pretending a ceiling covers it."
  },

  "_transport": [
    "A plain git fetch brings NO base — measured. Tag auto-following only takes tags pointing into fetched history, and these bases are detached.",
    "git push origin \"refs/tags/b12/corpus/*:refs/tags/b12/corpus/*\"",
    "git fetch origin \"refs/tags/b12/corpus/*:refs/tags/b12/corpus/*\"",
    "Then, ON THE MAC, before spending anything:  node scripts/b12-author.mjs verify-corpus b12-corpus/pilot-plan.json --deep"
  ]
}
