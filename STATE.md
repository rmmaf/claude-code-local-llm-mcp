# STATE.md

Overwritten every session, never appended — `git log -p STATE.md` is the diary.
Ceiling: 40 lines below this header.

## Where I stopped

**Everything the Mac needs is committed, and the Mac has not run.**

- **G-stop amended a second time** (`f219e63`): TWO value axes, the 15% governs one.
  The second is **output integrity**, and the amendment says what it cannot claim:
  **B16 judges the ADMISSION half only** (0 of 52), while the REFUSAL half is
  carried *without a threshold, deliberately* — `mac-16` observed over-refusal, and
  a threshold set after seeing it is the error B16 exists to correct. So **nothing
  here can return "these pre-flights refuse too much."** Caught by adversarial read,
  in my own first draft. Not *correctness*: that is B4's word and B4 is `moot`.
- **The instrument is repaired** (`4a7eaac`). `wouldHaveAdded` sets `R_hi⁺`, the
  FALL-side number, and differed from the scored numerator **three ways at once**:
  clamped where it is signed, uncapped where it is capped, and carrying a
  **turn-collapse term the frozen metric excludes by name** — so a refused row
  outweighed an identical credited one, partly by a tool-call argument. Plus
  `excludedForeignUnits`, `rows: CreditedRow[]`, a `requestIds?` subset seam.
- **Contract and oracle in** (`cb436ed`): 13 tests, 13 red, all `not implemented`.
  Specs and runner in (`dab8db9`). **Phase 3's threshold pre-registered**
  (`efbe17a`) **before a single unit was attempted**, which is what makes it one.

## Next action

**Run `bash scripts/b12-scorer-mac.sh` on the Mac**, clean tree, nothing else on the
box. It refuses on a dirty tree, on `rates.json` not byte-identical to `3541625`,
on a window under 32768, on a missing spec; it reads the window before AND after.

Send back the one `.tgz` it names. Then apply the bundle, review every body the
local model wrote **before** it reaches main, and write the `MEASUREMENTS.jsonl`
row against the pre-registered reading: **≥2 of 3** reachable · **0 of 3** amends
B12's text before Phase 4 · **exactly 1 of 3** inconclusive, manifest not sealable.

## Do not redo

- **A control never seen failing is not a control**, and two of mine could not.
  One reverted the wrong of two functions sharing a line; the other could not see
  a formula change, because the tests asserted `> 0` rather than a value.
- **Deleting `breakdownOfRequests`' subset filter left all 93 tests passing** — the
  seam `A_o` comes from. It had the parameter and a paragraph; never a test.
